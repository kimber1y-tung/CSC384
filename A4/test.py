import numpy as np
from tagger import *
# prior, transition, emission = train_HMM('data/train-public')
# min_ = float("inf")
# for key in emission:
#     if emission[key] < min_:
#         min_ = emission[key]
# print(min_)

# sol = np.load('soln.npz')
# print(sol['emission_val'])
